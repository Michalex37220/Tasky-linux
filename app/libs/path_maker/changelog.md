
### 23-10-2025

- *func* **core**.build_path construit le chemin absolu demandé et le renvoit

- *func* **image_path**.image_path et *func* path_maker.make_path construisent le chemin via un appel à *func* **core**.build_path

- suppression des fonctionalitées de logging
- make_path() peut à présent être appelé directement via un appel à path_maker (dossier)